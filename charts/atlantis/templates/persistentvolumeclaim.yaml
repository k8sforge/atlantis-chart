{{- if and .Values.enhancedStorage.enabled (not .Values.enhancedStorage.existingClaim) }}
{{- /* Auto-select storage configuration based on replica count */}}
{{- $replicaCount := .Values.atlantis.replicaCount | default 1 | int }}
{{- $storageClass := "" }}
{{- $accessMode := "" }}
{{- $size := "" }}

{{- if .Values.enhancedStorage.storageClass }}
  {{/* User specified exact storageClass - use as-is */}}
  {{- $storageClass = .Values.enhancedStorage.storageClass }}
{{- else }}
  {{/* Auto-select based on replica count */}}
  {{- if eq $replicaCount 1 }}
    {{- $storageClass = .Values.enhancedStorage.ebs.storageClass }}
  {{- else }}
    {{- $storageClass = .Values.enhancedStorage.efs.storageClass }}
  {{- end }}
{{- end }}

{{- if .Values.enhancedStorage.size }}
  {{/* User specified exact size - use as-is */}}
  {{- $size = .Values.enhancedStorage.size }}
{{- else }}
  {{/* Auto-select based on replica count */}}
  {{- if eq $replicaCount 1 }}
    {{- $size = .Values.enhancedStorage.ebs.size }}
  {{- else }}
    {{- $size = .Values.enhancedStorage.efs.size }}
  {{- end }}
{{- end }}

{{- if .Values.enhancedStorage.accessModes }}
  {{/* User specified exact accessModes - use as-is */}}
{{- else }}
  {{/* Auto-select based on replica count */}}
  {{- if eq $replicaCount 1 }}
    {{- $accessMode = "ReadWriteOnce" }}
  {{- else }}
    {{- $accessMode = "ReadWriteMany" }}
  {{- end }}
{{- end }}

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "atlantis.fullname" . }}-data
  labels:
    {{- include "atlantis.labels" . | nindent 4 }}
spec:
  accessModes:
    {{- if .Values.enhancedStorage.accessModes }}
    {{- range .Values.enhancedStorage.accessModes }}
    - {{ . | quote }}
    {{- end }}
    {{- else }}
    - {{ $accessMode | quote }}
    {{- end }}
  {{- if $storageClass }}
  {{- if (hasPrefix "-" $storageClass) }}
  storageClassName: ""
  {{- else }}
  storageClassName: {{ $storageClass | quote }}
  {{- end }}
  {{- end }}
  resources:
    requests:
      storage: {{ $size | quote }}
{{- end }}
